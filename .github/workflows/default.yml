name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Install prerequisites
      run: sudo apt install -y lcov
    - name: Build project
      run: ./build.sh
